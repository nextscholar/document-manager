version: "3.9"

services:
  # -----------------------------
  # Database (Postgres + Vector)
  # -----------------------------
  db:
    image: pgvector/pgvector:pg16
    container_name: docsach_db
    profiles:
      - prod
    environment:
      POSTGRES_USER: ${DB_USER:-appuser}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
      POSTGRES_DB: ${DB_NAME:-appdb}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_net

  # -----------------------------
  # Backend API
  # -----------------------------
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: docsach_api
    environment:
      DATABASE_URL: postgres://${DB_USER:-appuser}:${DB_PASSWORD:-changeme}@docsach_db:5432/${DB_NAME:-appdb}
      APP_ENV: ${APP_ENV:-development}
    depends_on:
      - docsach_db
    networks:
      - app_net

  # -----------------------------
  # Background Worker
  # -----------------------------
  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    container_name: example_worker
    environment:
      DATABASE_URL: postgres://${DB_USER:-appuser}:${DB_PASSWORD:-changeme}@docsach_db:5432/${DB_NAME:-appdb}
    depends_on:
      - docsach_db
    volumes:
      - worker_shared:/shared
    networks:
      - app_net

  # -----------------------------
  # Frontend
  # -----------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: docsach_frontend
    volumes:
      # For local dev hot-reload
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - docsach_api
    networks:
      - app_net

# -----------------------------
# Volumes
# -----------------------------
volumes:
  postgres_data:
  worker_shared:

# -----------------------------
# Networks
# -----------------------------
networks:
  app_net:
    driver: bridge
